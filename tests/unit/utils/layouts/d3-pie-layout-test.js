import layoutsD3PieLayout from 'dummy/utils/layouts/d3-pie-layout';
import { module, test } from 'qunit';

module('Unit | Utility | layouts/d3 pie layout');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = layoutsD3PieLayout();
  assert.ok(result);
});
