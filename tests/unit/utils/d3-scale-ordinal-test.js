import d3ScaleOrdinal from '../../../utils/d3-scale-ordinal';
import { module, test } from 'qunit';

module('Unit | Utility | d3 scale ordinal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3ScaleOrdinal();
  assert.ok(result);
});
