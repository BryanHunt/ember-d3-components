import plottersD3BarPlotter from '../../../utils/plotters/d3-bar-plotter';
import { module, test } from 'qunit';

module('Unit | Utility | plotters/d3 bar plotter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = plottersD3BarPlotter();
  assert.ok(result);
});
