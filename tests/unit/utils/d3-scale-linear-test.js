import d3ScaleLinear from '../../../utils/d3-scale-linear';
import { module, test } from 'qunit';

module('Unit | Utility | d3 scale linear');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3ScaleLinear();
  assert.ok(result);
});
