import d3Scale from '../../../utils/d3-scale';
import { module, test } from 'qunit';

module('Unit | Utility | d3 scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Scale();
  assert.ok(result);
});
