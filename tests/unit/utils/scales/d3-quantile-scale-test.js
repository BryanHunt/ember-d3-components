import scalesD3QuantileScale from '../../../utils/scales/d3-quantile-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 quantile scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3QuantileScale();
  assert.ok(result);
});
