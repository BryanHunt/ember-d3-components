import scalesD3TimeScale from '../../../utils/scales/d3-time-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 time scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3TimeScale();
  assert.ok(result);
});
