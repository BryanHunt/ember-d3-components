import d3LogScale from '../../../utils/d3-log-scale';
import { module, test } from 'qunit';

module('Unit | Utility | d3 log scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3LogScale();
  assert.ok(result);
});
