import scalesD3QuantizeScale from 'dummy/utils/scales/d3-quantize-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 quantize scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3QuantizeScale.create();
  assert.ok(result);
});
