import scalesD3OrdinalScale from '../../../utils/scales/d3-ordinal-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 ordinal scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3OrdinalScale();
  assert.ok(result);
});
