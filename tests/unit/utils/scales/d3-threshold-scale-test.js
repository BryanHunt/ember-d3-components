import scalesD3ThresholdScale from 'dummy/utils/scales/d3-threshold-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 threshold scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3ThresholdScale.create();
  assert.ok(result);
});
