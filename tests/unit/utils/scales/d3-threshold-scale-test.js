import scalesD3ThresholdScale from '../../../utils/scales/d3-threshold-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 threshold scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3ThresholdScale();
  assert.ok(result);
});
