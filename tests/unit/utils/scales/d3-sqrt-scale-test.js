import scalesD3SqrtScale from '../../../utils/scales/d3-sqrt-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 sqrt scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3SqrtScale();
  assert.ok(result);
});
