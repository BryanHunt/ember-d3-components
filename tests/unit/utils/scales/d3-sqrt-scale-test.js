import scalesD3SqrtScale from 'dummy/utils/scales/d3-sqrt-scale';
import { module, test } from 'qunit';

module('Unit | Utility | scales/d3 sqrt scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scalesD3SqrtScale.create();
  assert.ok(result);
});
